# 能力与兼容性边界 · 0.4.0

已实现显式邻接支持计数、加权 Shannon 熵、惰性最小堆与迭代回溯；重叠图案使用无碰撞整数键和边缘签名。支持完整输出重建、周期接缝、像素 pins、候选限制、浮点权重和明确的无解/预算错误。

PNG 输入/输出、Node 可终止 Worker、UTF-8 JSON 文件与标准输入、同目录临时文件/防覆盖写出已实现。XML 支持六种 D4 对称、邻接方向、权重、unique 多方向图片和 subsets。网页支持本地 PNG 选择、参数调整、取消、保存和 JSON 调试。

266 个官方对照只证明相应夹具的图案顺序、频率、四向邻接、瓦片标签与方向位图一致；它们不等价于完整上游套件。四组生成性能任务的所有成功输出均逐个局部窗口对照官方图案；不同 RNG 和回溯策略不追求同种子逐像素相同。

## 保留差距

1. 上游 `samples.xml` 的多任务、多截图、限制次数、黑底未确定像素等批处理工作流未实现；网页尚无瓦片 XML/多文件素材管理。
2. 目前仅加权熵选择；Scanline/MRV 等启发式、逐步观察/调试和精细矛盾来源尚缺。
3. `ground` 已有 API，但本轮官方对照固定 ground=false；不能将其列为已验证完整 ground 兼容。像素 pins 是本地扩展。
4. 4096 图案、2000000 状态、4000000 邻接和工作预算是显式边界，不是无限世界或任意大型素材。高连通度素材可能先触及邻接上限。
5. PNG 只保留颜色像素；不保留元数据、动画或色彩配置。浏览器 canvas 不承诺保留透明像素隐藏 RGB；Node PNG 路径已验证精确 RGBA。没有图像浏览/版权素材库。
6. 四组 Windows 同机结果仍慢于参考 C#；测量范围与运行时不同。没有峰值内存、所有平台、长时间或生产负载结论。
7. 上游参考项目要求 .NET 10，本轮使用未改 C# 核心 + 原创适配 harness，在 .NET 9.0.13 重编译执行；不声称测试了官方发行二进制或完整官方项目。

本地 CI 文件、独立库/API/CLI/网页交付已具备，但没有远端 CI、发布、比赛验收或全面追平结论。
